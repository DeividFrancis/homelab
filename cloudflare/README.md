 1. Usar o cli do cloudflared
 2. Fazer login
 3. Criar um cluster via cli (via ui não funcionou nos testes)
 4. Pegar as credenciais e alimentar no vault
 5. Na UI da cloudflare converter o tunnel para gerenciavel para registrar os hosts (via config.yml nao funcionou)